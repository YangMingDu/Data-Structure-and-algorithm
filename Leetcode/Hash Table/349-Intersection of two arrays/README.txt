
给定两个数组，编写一个函数来计算它们的交集。

 

示例 1：

输入：nums1 = [1,2,2,1], nums2 = [2,2]
输出：[2]
示例 2：

输入：nums1 = [4,9,5], nums2 = [9,4,9,8,4]
输出：[9,4]
 

说明：

输出结果中的每个元素一定是唯一的。
我们可以不考虑输出结果的顺序。

思路：利用set的特性来解决问题，将nums1全都加到一个set中，再遍历nums2的所有元素，尝试在set中寻找，若找到，说明是交集，将其insert到结果set中，最后将结果set建立为vector输出。