给定一个数组nums，写一个函数将数组中所有的0挪到函数的末尾维持其他元素的相对位置。


思路1：将所有非0元素放到一个数组里，之后将后面的元素（根据数组长度）都赋值为0
思路2：设置一个区间[0，k)存储所有非0元素，从0开始遍历数组，碰到非0元素，将其与索引为k的元素进行赋值，然后k指向下一个元素，当遍历结束时，将k到最后一个元素都赋值为0即可。
思路3：遍历所有数组元素，每次发现非零元素时都与第k个元素进行交换，k初始化为0。
优化思路3：只有当i不等于k时才进行置换，当元素全非0时，这样操作避免了多次无用置换。