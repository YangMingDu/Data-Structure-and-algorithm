优化插入排序:

原插入排序由于每次当arr[i]不在合适位置时都要运用swap函数与前一元素进行交换，这样增加了排序时间，优化后的插入排序，无需每次都要使用交换函数，而是运用赋值，将arr[i]赋值给e，用e和前面元素进行比较，如果不在合适位置就将当前位置后移（也是运用赋值），当找到合适位置j的时候，将e赋值给arr[j]，节约了交换的时间，通过程序排序所需时间对比，在元素个数为10000时，运行速度提高三倍。