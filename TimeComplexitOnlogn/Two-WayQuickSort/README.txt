双路快速排序：

由于在有大量重复元素的数组中，使用快速排序会把数组分成两个极其不平衡的部分，在这种情况下，快排也会退化为O（n²）的算法，因此使用双路快排。

双路快排主要思想：

双路：从left+1元素（最左边除了标定点）和right元素（最右边的元素）向中间扫描（i++,j--），当左边碰到大于标定点的时候，与在右边碰到小于标定点的元素互换位置，直到i和j相遇，因为标定点在左边，所以将标定点与j位置的元素进行调换即可。