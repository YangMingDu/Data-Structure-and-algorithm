优化堆排序：

知识点：对于一个完全二叉树来说，第一个非叶子节点（有孩子）的索引是这个完全二叉树的元素个数除以二得到的索引值（索引从1开始计算）。

首先写一个新的构造函数,利用知识点，将数组直接传入堆中，在第count/2的元素开始进行shiftDown操作，可以直接得到一个最大堆。

经过优化，发现还是不如快速排序，所以在系统级别很少使用堆排序，堆主要在动态数据维护。

将n个元素逐一插入到一个空堆中，算法复杂度O（nlogn），heapify的过程为O（n）。